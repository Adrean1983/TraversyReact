// Make a class component called Todos. Make a <h1> tag called Todos and display

// Add three todo items using state. They have a titled and completed. Pass the state to App and todos and display. Need to add a TodoItem Class Component (display in a <p> The title of these componets)

// Add Css styling to the todoItems component. Use a function. 
// Conditionally show a line through background: '#f4f4f4',padding: '10px',borderBottom: '1px #ccc dotted',

// Add a checkbox to the todo items. And a method to mark items complete markComplete and console.log("hello"). 
// Climb the ladder to the App.js

// in todoItems bind the id. can use destructuring.

// Write the mark items complete -markComplete- function in app.js. (need map function)

// Write a function that adds a delete button to the inputs. Add style to button.

// Make a layout folder with a file called header.js. It's a functional component. Put a H1 tag. Add this styling   
//   color: "#fff", backgroundColor: "#333", textAlign: "center", padding: "10px"

// Add a class component called AddTodo. It's form with an input and a submit. It has some inline flex styling.

// Import AddTodo to App.js and render. Then put everything being rendered in a div with classname container. Go to app.css global and add following style     

// .Container {
//     padding: 0 1rem;
// }

// .btn {
// display: inline-block;
// border: none;
// background: #555;
// color: #fff;
// padding: 7px 20px;
// cursor: pointer;
// }

// .btn:hover {
// background: #666;
// }

// Add a function in AddTodo called onChange to handle onChange events. Need to add a submit event too, have to use e.preventDefault(); Send title up to app with addTodo and set it back to blank. call 

// In app Js add the addTodo function.
